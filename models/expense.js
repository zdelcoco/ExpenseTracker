class Expense {
    constructor(id, title, date, amount, deleted) {
        this.id = id;
        this.title = title;
        this.date = date;
        this.amount = amount;
        this.deleted = deleted;
    }
}

export default Expense;